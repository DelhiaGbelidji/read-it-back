version: '3.3'
services:
  nest-api:
    container_name: nest-api
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./prisma:/app/prisma
      - ./dev.db:/app/dev.db:rw
      - .:/app
      - /app/node_modules
